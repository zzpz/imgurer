fastapi # api
uvicorn # web server
alembic # db migrations
sqlalchemy  # ORM
sqlalchemy-utils    # ORM 2
jinja2  # javascript templates
python-multipart # forms and files
python-jose[cryptography] # JWT + security
passlib[bcrypt] # hashing passwords
pillow  # image manipulation
pybktree # tree storage for ~ O(LogN) search of closeness
aiofiles # for serving our static files from disk
psycopg2 #alembic migrations
#python-dotenv #for reading our .env file
#aiohttp # async requests
requests #sync requests
#dhash #for computing hashes
